name: Use Secret

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Use env
        env:
          API_KEY: ${{ secrets.MY_SECRET_THING }}
        run: echo "Using API key"
